# Run the FPC 10a5:9201 implementation behind fprintd's standard
# D-Bus-activated service.  Reset ExecStart because systemd only permits one
# command for a Type=dbus service.
[Service]
ExecStart=
ExecStart=/usr/local/bin/fingerprint-ocv --bus=system
Restart=on-failure
RestartSec=2s
